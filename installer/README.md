StudySync installer scaffold

This folder contains a minimal scaffold showing how to package the app for distribution.

Files:
- MakeInstaller.sh — example script that zips the app and produces a distributable archive.
- resources/ — contains icon and other installer assets.

Note: Building real platform installers (MSI/NSIS/DMG) requires platform-specific tooling and the actual application binaries.
